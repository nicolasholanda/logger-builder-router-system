spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/loggerdb
    username: logger
    password: logger
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration
